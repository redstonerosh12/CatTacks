class Bus:
    def __init__(self, name, timings, destinations):
        self.name = name
        self.timings = timings
        self.destinations = destinations

if __name__ == '__main__':
    print("Welcome to Bus, Bus is not an executable")